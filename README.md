🌐 Internet Connection Status Detector

This project is a simple and interactive Internet Connection Status Detector built using HTML, CSS, and JavaScript.
It detects the user’s current network connectivity, displays the device’s public IP address, and shows the approximate network strength using the browser’s Network Information API.


🚀 Features
✅ 1. Real-time Internet Status

Detects whether the user is Online or Offline

Updates instantly when connection changes

✅ 2. Fetches Public IP Address

Uses the API:

https://api.ipify.org?format=json


To display the user's public IP address.

✅ 3. Network Strength Detection

Uses navigator.connection.downlink

Shows approximate download speed in Mbps

If not supported by the browser, shows "Unknown"

✅ 4. Clean and Simple UI

Displays connection status

Color-coded results

Good for beginners learning API calls and browser events
